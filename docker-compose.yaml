services:
  database:
    image: postgres
    ports:
      - 5434:5432
    volumes:
      - ./postgres_data:/var/lib/postgresql
    environment:
      - POSTGRES_USER=store
      - POSTGRES_PASSWORD=123456
      - POSTGRES_DB=ecommerce